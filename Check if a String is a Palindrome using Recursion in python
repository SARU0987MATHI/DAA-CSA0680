def is_palindrome(s):
    s = ''.join(s.lower().split())
    return s == s[::-1]


input_string = "A man a plan a canal Panama"
print(f"{input_string} is {'a palindrome' if is_palindrome(input_string) else 'not a palindrome'}.")
